spring.application.name=Smart Medicine Reminder

# Database configuration (Cloud-Ready)
# Falls back to Railway Public Proxy if Environment Variables are missing
spring.datasource.url=${MYSQL_URL:jdbc:mysql://hopper.proxy.rlwy.net:31277/railway?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC}
spring.datasource.username=${MYSQL_USER:root}
spring.datasource.password=${MYSQL_PASSWORD:QglQVXDIPjKChdXFQZtvjVskckJwpyIW}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
# Removing hardcoded dialect to allow Spring to auto-detect MySQL 8 on Railway
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# File Upload sizes
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# App config
app.upload.dir=uploads

# Server Port
server.port=${PORT:8080}

# Mail Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=dudipalamrudula@gmail.com
spring.mail.password=djvf edzp riad liic
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Twilio SMS Configuration
twilio.account.sid=YOUR_TWILIO_ACCOUNT_SID
twilio.auth.token=YOUR_TWILIO_AUTH_TOKEN
twilio.phone.number=YOUR_TWILIO_PHONE_NUMBER