spring.application.name=Smart Medicine Reminder

# Database configuration
spring.datasource.url=jdbc:mysql://localhost:3306/med_reminder_db?createDatabaseIfNotExist=true&allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC
spring.datasource.username=dudipalamrudula
spring.datasource.password=Mrudulareddy@1295
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# File Upload sizes
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# App config
app.upload.dir=uploads

# Mail Configuration (Set these to enable real email sending)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=dudipalamrudula@gmail.com
spring.mail.password=djvf edzp riad liic
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Twilio SMS Configuration (Set these to enable real SMS sending)
twilio.account.sid=YOUR_TWILIO_ACCOUNT_SID
twilio.auth.token=YOUR_TWILIO_AUTH_TOKEN
twilio.phone.number=YOUR_TWILIO_PHONE_NUMBER

server.port=${PORT:8080}

spring.datasource.url=${MYSQL_URL:jdbc:mysql://localhost:3306/med_reminder_db}
spring.datasource.username=${MYSQL_USER:dudipalamrudula}
spring.datasource.password=${MYSQL_PASSWORD:Mrudulareddy@1295}

spring.jpa.hibernate.ddl-auto=update